<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>de.heuboe.asfinag</groupId>
        <artifactId>vmis2-tls-resources</artifactId>
        <version>3.3.0</version>
    </parent>

    <artifactId>vmis2-tls-proto-interface</artifactId>
    <description>VMIS2 Library with protos for TLS.</description>

    <properties>
        <heuboe.pdb.changelogfile>${project.parent.basedir}/CHANGELOG.md</heuboe.pdb.changelogfile>
        <protoDir>${project.parent.basedir}/vmis2-tls-proto-interface/src/main/proto/</protoDir>
    </properties>

    <dependencies>
        <dependency>
            <groupId>de.heuboe.asfinag</groupId>
            <artifactId>vmis2-jprotoc-transferinterface</artifactId>
            <version>${jprotoc.version}</version>
        </dependency>
        <!-- provided dependencies (because compile-time only or provided otherwise) -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.data</groupId>
            <artifactId>spring-data-commons</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.data</groupId>
            <artifactId>spring-data-mongodb</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <!-- Annotation processor used to build ProtoTransferCatalog lists for the ServiceLoader -->
            <groupId>org.atteo.classindex</groupId>
            <artifactId>classindex</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- optional dependencies (in case the proto files contain service definitions) -->
        <dependency>
            <groupId>io.grpc</groupId>
            <artifactId>grpc-protobuf</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>io.grpc</groupId>
            <artifactId>grpc-stub</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <extensions>
            <extension>
                <groupId>kr.motd.maven</groupId>
                <artifactId>os-maven-plugin</artifactId>
                <version>${os-maven-plugin.version}</version>
            </extension>
        </extensions>
        <plugins>
            <!-- generate proto files -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.0.0</version>
                <dependencies>
                    <dependency>
                        <groupId>de.heuboe.asfinag</groupId>
                        <artifactId>vmis2-tls-generator-proto</artifactId>
                        <version>${project.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.apache.logging.log4j</groupId>
                        <artifactId>log4j-slf4j-impl</artifactId>
                        <version>2.13.3</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>generate-proto-files</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>java</goal>
                        </goals>
                        <configuration>
                            <includePluginDependencies>true</includePluginDependencies>
                            <mainClass>de.heuboe.asfinag.vmis2.tls.resources.generator.proto.Vmis2ProtoGenerator
                            </mainClass>
                            <arguments>
                                <argument>${protoDir}</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Update proto-files with the correct interfaceVersion -->
            <plugin>
                <groupId>de.heuboe.asfinag.maven.plugins</groupId>
                <artifactId>vmis2-protoversion-maven-plugin</artifactId>
                <version>${vmis2-protoversion-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>add-protointerface-version</id>
                        <goals>
                            <goal>upsert</goal>
                        </goals>
                        <phase>generate-sources</phase>
                    </execution>
                </executions>
                <configuration>
                    <directories>${protoDir}</directories>
                </configuration>
            </plugin>
            <!--
                compile *.proto files
            -->
            <plugin>
                <groupId>org.xolstice.maven.plugins</groupId>
                <artifactId>protobuf-maven-plugin</artifactId>
                <!-- common configuration for all executions -->
                <configuration>
                    <clearOutputDirectory>false</clearOutputDirectory>
                    <protocArtifact>com.google.protobuf:protoc:${protobuf.version}:exe:${os.detected.classifier}
                    </protocArtifact>
                    <useArgumentFile>true</useArgumentFile>
                </configuration>
                <executions>
                    <!-- execution to compile the proto files without grpc plugin; also attach '*.proto' files
                        and generate DescriptorSet file ('*.protobin') (must be done only once because otherwise it will
                        be attached twice to the build and the double 'deploy' of the same protobin artifact fails) -->
                    <execution>
                        <id>protobuf-compile</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <attachProtoSources>true</attachProtoSources>
                            <writeDescriptorSet>true</writeDescriptorSet>
                            <includeSourceInfoInDescriptorSet>true</includeSourceInfoInDescriptorSet>
                            <includeDependenciesInDescriptorSet>true</includeDependenciesInDescriptorSet>
                            <attachDescriptorSet>true</attachDescriptorSet>
                            <descriptorSetClassifier>descriptorSet</descriptorSetClassifier>
                            <protocPlugins>
                                <protocPlugin>
                                    <id>vmis2</id>
                                    <groupId>de.heuboe.asfinag</groupId>
                                    <artifactId>vmis2-jprotoc-plugin</artifactId>
                                    <version>${jprotoc.version}</version>
                                    <mainClass>de.heuboe.vmis2.jprotoc.Proto2PojoGenerator</mainClass>
                                    <args>
                                        <arg>--assertVersion=${project.version}</arg>
                                    </args>
                                </protocPlugin>
                            </protocPlugins>
                        </configuration>
                    </execution>
                    <execution>
                        <id>protobuf-test-compile</id>
                        <phase>generate-test-sources</phase>
                        <goals>
                            <goal>test-compile</goal>
                        </goals>
                        <configuration>
                            <attachProtoSources>false</attachProtoSources>
                            <writeDescriptorSet>false</writeDescriptorSet>
                            <protocPlugins>
                                <protocPlugin>
                                    <id>vmis2</id>
                                    <groupId>de.heuboe.asfinag</groupId>
                                    <artifactId>vmis2-jprotoc-plugin</artifactId>
                                    <version>${jprotoc.version}</version>
                                    <mainClass>de.heuboe.vmis2.jprotoc.Proto2PojoGenerator</mainClass>
                                </protocPlugin>
                            </protocPlugins>
                        </configuration>
                    </execution>
                    <!-- execution to compile the proto files with the grpc plugin -->
                    <execution>
                        <id>protobuf-compile-custom</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>compile-custom</goal>
                            <goal>test-compile-custom</goal>
                        </goals>
                        <configuration>
                            <attachProtoSources>false</attachProtoSources>
                            <writeDescriptorSet>false</writeDescriptorSet>
                            <pluginId>grpc-java</pluginId>
                            <pluginArtifact>io.grpc:protoc-gen-grpc-java:${grpc.version}:exe:${os.detected.classifier}
                            </pluginArtifact>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--
                place the generated FileDescriptor file (*.protobin) in the main artifact
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>add-protobin-resource</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>add-resource</goal>
                        </goals>
                        <configuration>
                            <resources>
                                <resource>
                                    <directory>${project.build.directory}/generated-resources/protobuf/descriptor-sets
                                    </directory>
                                    <includes>
                                        <include>${project.build.finalName}.protobin</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!-- Execute release: mvn clean release:clean release:prepare release:perform -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <configuration>
                    <completionGoals>vmis2-protoversion:upsert</completionGoals>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
