<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>de.heuboe.datex2</groupId>
	<artifactId>mstBuilderLib_idl</artifactId>
	<version>3.6.1</version>
	<parent>
		<artifactId>jhbpom</artifactId>
		<groupId>de.heuboe.base</groupId>
		<version>4.4.2</version>
	</parent>
	
	
	<properties>
		<maven.compiler.target>1.7</maven.compiler.target>
		<maven.compiler.source>1.7</maven.compiler.source>
        <pkgPrefix>-pkgTranslate mstBuilder de.heuboe.datex2.mstBuilder</pkgPrefix>
        <idlDir>${project.basedir}</idlDir>
        <buildDir>${project.build.directory}/generated-sources/main/java</buildDir>
    </properties>
    <build>
        <plugins>
            <!-- Ant run plugin (das idl plugin bringts nicht, das kann keine #includes) -->
            <!-- baut auch gleich das .jar-file mit den .idls -->
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.7</version>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <configuration>
                            <target>
                                <property name="file.encoding" value="UTF-8"/>
                            </target>                        
					        <encoding>UTF-8</encoding>
                            <tasks>
                                <exec executable="${env.JAVA_HOME}/bin/idlj.exe">
                                    <arg
                                        line="-v -fall ${pkgPrefix} -td ${buildDir} ${idlDir}/mstBuilder.idl" />
                                </exec>
                                <jar destfile="${project.build.directory}/idls.jar">
                                    <fileset dir="${idlDir}" includes="*.idl" />
                                </jar>
                            </tasks>
                            <sourceRoot>
                                ${project.build.directory}/generated-sources/main/java </sourceRoot>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--
                MAVEN-PAX-PLUGIN (wraps BND - Bundle Tool
                http://www.aqute.biz/Code/Bnd)
            -->
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <version>2.1.0</version>
                <!--
                    The following instructions build a simple set of
                    public/private classes into an OSGi bundle
                -->
                <configuration>
                    <instructions>
                        <Bundle-SymbolicName>${project.groupId}.${project.artifactId}</Bundle-SymbolicName>
                        <Bundle-Version>${project.version}</Bundle-Version>
                        <Export-Package>de.heuboe.datex2.mstBuilder;version="${project.version}"</Export-Package>
                    </instructions>
                </configuration>
                <executions>
                    <execution>
                        <id>bundle-manifest</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>manifest</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--
                Kopiert das .jar-file mit den .idls ins Repository
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>1.5</version>
                <executions>
                    <execution>
                        <id>attach-artifacts</id>
                        <phase>package</phase>
                        <goals>
                            <goal>attach-artifact</goal>
                        </goals>
                        <configuration>
                            <artifacts>
                                <artifact>
                                    <file>${project.build.directory}/idls.jar</file>
                                    <type>jar</type>
                                    <classifier>idl</classifier>
                                </artifact>
                            </artifacts>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
     </build>
     
</project>