1) 

Der Aufruf von importCfg.cmd schreibt das MDM-Datex2-Verkehrsdaten-Mapping (data/mstConfig.xml) 
in die Datenbank  (Collection datex2MSTConfig)


2) 

importDef*.cmd schreiben MST-Definitionen (Collection datex2MSTDefinition). 
Dabei ist die Version als Aufruf-Parameter anzugeben. 
